import Vue from 'vue'
import Vuex from 'vuex'
import playerStore from './player.store'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    playerStore
  }
})
